apiVersion: v1
kind: Service
metadata:
  name: { { include "nameService" . } }
spec:
  selector:
    app: { { include "nameDeployment" . } }
  type: ClusterIP
  ports:
    - port: { { .Values.laravelPort } }
      targetPort: { { .Values.laravelPort } }
      protocol: TCP

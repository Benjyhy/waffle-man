apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mysql-svc
spec:
  selector:
    app: {{ .Release.Name }}-mysql-dep
  ports:
    - protocol: TCP
      {{- with .Values.mysql}}
      port: {{ .port }}
      targetPort: {{ .port }}
      {{- end }}

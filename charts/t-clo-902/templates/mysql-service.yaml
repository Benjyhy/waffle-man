apiVersion: v1
kind: Service
metadata:
  name: {{ include "t-clo-902.nameService" . }}
spec:
  selector:
    app: {{ .Chart.Name }}-{{ .Release.Name }}-deployment
  ports:
    - protocol: TCP
      {{- with .Values.mysql}}
      port: {{ .mysqlPort }}
      target: {{ .mysqlPort }}
      {{- end }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "t-clo-902.nameDeployment" . }}
  labels:
    app: {{ include "t-clo-902.nameDeployment" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "t-clo-902.nameDeployment" . }}
  template:
    metadata:
      labels:
        app: {{ include "t-clo-902.nameDeployment" . }}
    spec:
      initContainers:
        - name: migrations
          image: {{ .Values.t-clo-902.ghcrRepo }}{{ .Chart.Name }}:latest
          command:
            - php
          args:
            - artisan
            - migrate
            - --force
          envFrom:
          - configMapRef:
              name: {{ include "t-clo-902.nameConfigmap" . }}
          - secretRef:
              name: {{ include "t-clo-902.nameSecret" . }}
      containers:
        - name: back
          image: {{ .Values.t-clo-902.ghcrRepo }}{{ .Chart.Name }}:latest
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.back.laravelPort }}
          envFrom:
          - configMapRef:
              name: {{ include "t-clo-902.nameConfigmap" . }}
          - secretRef:
              name: {{ include "t-clo-902.nameSecret" . }}
      {{ include "t-clo-902.imagePullSecret" . }}

apiVersion: v1
kind: Service
metadata:
  name: { { include "t-clo-902.nameService" . } }
spec:
  selector:
    app: { { include "t-clo-902.nameDeployment" . } }
  type: ClusterIP
  ports:
    - port: { { .Values.back.laravelPort } }
      targetPort: { { .Values.back.laravelPort } }
      protocol: TCP

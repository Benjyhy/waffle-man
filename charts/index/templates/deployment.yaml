apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nameDeployment" . }}
  labels:
    app: {{ include "nameDeployment" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "nameDeployment" . }}
  template:
    metadata:
      labels:
        app: {{ include "nameDeployment" . }}
    spec:
      containers:
        - name: {{ include "nameDeployment" . }}
          image: {{ .Values.global.ghcrRepo }}{{ .Chart.Name }}:latest
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.port }}
          env:
            - name: ELASTICSEARCH_URI
              valueFrom:
                secretKeyRef:
                  name: { { include "nameSecret" . } }
                  key: eck_uri
            - name: AMQP_URI
              valueFrom:
                secretKeyRef:
                  name: { { include "nameSecret" . } }
                  key: amqp_uri
      {{ include "imagePullSecret" . }}
